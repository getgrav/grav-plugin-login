enabled: true
built_in_css: true
route: false

oauth:
  enabled: false
  access:
    site:
      login: true

  providers:
    Facebook:
      enabled: false
      credentials:
        key:
        secret:

    Google:
      enabled: false
      credentials:
        key:
        secret:

    GitHub:
      enabled: false
      credentials:
        key:
        secret:

    Twitter:
      enabled: false
      credentials:
        key:
        secret:
