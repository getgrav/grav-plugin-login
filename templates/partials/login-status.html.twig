<span class="login-status">
    {% if grav.user.username %}
        Welcome <strong>{{ grav.user.username }}</strong>, <a class="logout" href="{{ uri.addNonce(uri.url ~ '/task:login.logout', 'logout-form', 'logout-nonce') }}">Logout</a>
    {% endif %}
</span>
